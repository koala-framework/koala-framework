var loaderUtils = require("loader-utils");

module.exports = function(source, map) {
    this.cacheable();

    var prependCode = "var jQuery = require('jquery');\n";

    return prependCode + source;
};
