"use strict";
const glob = require("glob");

module.exports = function() {
    let ret = [];
    glob.sync("vendor/*/*/trl/en.po").forEach(i => {
        //in vendor code language is en, thus en.po must always exist
        ret.push(i.replace(/\/en\.po$/, "/[language].po"));
    });
    return ret;
};
