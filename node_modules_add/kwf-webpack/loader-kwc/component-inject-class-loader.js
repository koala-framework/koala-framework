var loaderUtils = require("loader-utils");

module.exports = function(source, map) {
    var query = loaderUtils.parseQuery(this.query);

    if (!query.cls) {
        this.emitError("Missing loader parameter cls");
        return;
    }

    this.cacheable();

    return '$componentClass: "'+query.cls+'";\n'+source;
};
