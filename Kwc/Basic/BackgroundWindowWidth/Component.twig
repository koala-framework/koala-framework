<div class="{{ cssClass }} {{ backgroundColor }}">
    {% if image.hasContent() %}
        {{ renderer.component(image) }}
    {% endif %}
    <div class="background{% if image.hasContent() %} opaque{% endif %}"></div>
    <div class="content">
        {{ renderer.component(paragraphs) }}
    </div>
</div>
{% if marginBottom %}
<div class="space" style="height: {{ marginBottom }}px"></div>
{% endif %}