<figure class="{{ rootElementClass }} kwfUp-kwcImage" style="{{ style }}" data-width="100%" data-max-width="{{ maxWidth }}">
    {% if baseUrl %}
        <div class="{{ "outerContainer"|bemClass }}">
            <div class="{{ containerClass }}" style="padding-bottom: {{aspectRatio}}%"
                data-min-width="{{ minWidth }}"
                data-max-width="{{ maxWidth }}"
                data-src="{{ baseUrl }}">
                <noscript>
                    {{ renderer.image(image, altText, imgAttributes) }}
                </noscript>
            </div>
        </div>
        {% if showImageCaption %}
            <figcaption class="{{ "imageCaption"|bemClass }} kwfUp-imageCaption" style="{{ captionStyle }}"><span>{{ image_caption }}</span></figcaption>
        {% endif %}
    {% endif %}
</figure>
