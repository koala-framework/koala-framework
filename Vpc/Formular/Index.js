Ext.namespace('Vpc.Formular');
Vpc.Formular.Index = function(renderTo, config)
{
    this.grid = new Vpc.Paragraphs.Index(renderTo, config);
};


Ext.extend(Vpc.Formular.Index, Ext.util.Observable,
{
})