Ext2.namespace('Kwf.Maintenance');

Kwf.Maintenance.Viewport = Ext2.extend(Ext2.Viewport, {
    layout: 'fit',
    initComponent: function()
    {
        Kwf.Maintenance.Viewport.superclass.initComponent.call(this);
    }
});
