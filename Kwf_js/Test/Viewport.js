Ext.namespace('Kwf.Test');

Kwf.Test.Viewport = Ext.extend(Ext.Viewport, {
	layout: 'fit',
    initComponent: function()
    {
        Kwf.Test.Viewport.superclass.initComponent.call(this);
    }
});
